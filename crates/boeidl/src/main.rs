//! boeidl CLI — stub. Real commands (compile/check/inspect) land in a later batch.

fn main() {
    eprintln!("boeidl: CLI not implemented yet (batch 1 = parser for `model` block only)");
    std::process::exit(2);
}
